-- System-specific absolute paths that plugins may need (LSP roots, SDKs, etc).
-- Update this file when porting to a different machine instead of hunting
-- through plugin configs for hardcoded paths.
return {
	odin_builtin = '/data/dev/ext/Odin/base/builtin',
}
